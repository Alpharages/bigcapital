{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build",
  "outputDirectory": "packages/server/build",
  "builds": [
    {
      "src": ".src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [{
    "source": "/(.*)",
    "destination": "/"
  }
  ],
  "build": {
    "env": {
      "NODE_VERSION": "18.x"
    }
  }
}